name: Build

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: contact-api/.github/.github/actions/setup@v1.0.0

      - name: TypeScript typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build
